motor auditor, ejecuta `doc/governance_prompts/96.3 investigacion multiagente a input M0.md`.

Input M0 MD a investigar:
`doc/_drafts/input_m0_handoff_para_claude_<initiative_short_id>.md`

Objetivo:
verificar, tensionar y compilar adversarialmente ese input M0 existente, sin
abrir `plan.md`, sin implementar codigo y sin crear artefactos formales de
iniciativa.

Reglas de ejecucion:
- aceptar el MD indicado aunque existan otros drafts en `doc/_drafts/`;
- usar roadmap vivo `25`, `25.1` y `25.2`; no usar `34` como autoridad;
- extraer y cerrar Claim Ledger;
- ejecutar agentes base y condicionales bajo cap 6;
- respetar frontera `input_m0 != plan.md != F2 != F4`;
- no automatizar el lanzamiento de motor activo F1.

Salida esperada:
`Decision = LISTO_PARA_CLAUDE_F1` o `BLOQUEADO`.

Si `Decision = LISTO_PARA_CLAUDE_F1`:
- escribir o actualizar el MD final para motor activo F1 en:
  `doc/_drafts/input_m0_handoff_para_claude_<initiative_short_id>.md`;
- devolver la ruta escrita y numero de lineas;
- devolver la llave de chat corta para pegar en un nuevo chat de motor activo F1;
- mantener `AGENTE_M4_ACTIVO` + `HANDOFF_M0_A_F1` dentro del MD final.

Si `Decision = BLOQUEADO`:
- no producir MD final incompleto;
- indicar causa, precondicion faltante, evidencia y siguiente paso minimo
  seguro.
